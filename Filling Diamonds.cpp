#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int casos, n;

    cin >> casos;

    while(casos --){
        cin >> n;
        cout << n << "\n";
    }

    return 0;
}